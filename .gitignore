*.pyc
__pycache__/
.vscode/
# Card art is too large to track. Ignore the *contents* rather than the
# directory itself — git won't descend into an ignored directory, so the
# negation below only works against a `dir/*` pattern.
static/images/*
!static/images/card-placeholder.svg
static/media/
venv/
integration/*
flask-port/*
screenshots/*
uploads/*
venv/*
node_modules/
screenshot*
check_page*
_edge_tmp/
_panel_preview.html
reading*.png
spread*.png
v_*.png
