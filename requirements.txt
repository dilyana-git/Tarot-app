# Security floor: audited clean with `pip-audit -r requirements.txt` (2026-07-29).
# Werkzeug is a Flask transitive dep, pinned explicitly to hold that floor.
Flask==3.1.3
Werkzeug==3.1.8
gunicorn==26.0.0
